{
    "version": 2,
    "builds": [{
        "src": "voice_call_project/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "voice_call_project/wsgi.py"
        }
    ],
    "env": {
        "PYTHON_VERSION": "3.7"
      }
}