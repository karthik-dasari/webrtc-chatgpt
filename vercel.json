{
    "version": 2,
    "builds": [{
        "src": "video_call_project/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "video_call_project/wsgi.py"
        }
    ]
}